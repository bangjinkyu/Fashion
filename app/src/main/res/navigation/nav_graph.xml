<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/homeFragment">
    <fragment
        android:id="@+id/homeFragment"
        android:name="com.room.fashion.home.HomeFragment"
        android:label="@string/home_fragment_label"
        tools:layout="@layout/fragment_home">
    </fragment>

    <fragment
        android:id="@+id/favoriteFragment"
        android:name="com.room.fashion.favorite.FavoriteFragment"
        android:label="@string/favorite_fragment_label"
        tools:layout="@layout/fragment_favorite">
    </fragment>

    <fragment
        android:id="@+id/fashionFragment"
        android:name="com.room.fashion.fashion.FashionFragment"
        android:label="@string/favorite_fragment_label"
        tools:layout="@layout/fragment_fashion">
    </fragment>
</navigation>